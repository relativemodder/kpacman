import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

Rectangle {
    color: palette.base
    Text {
        text: qsTr("Settings page")
        anchors.centerIn: parent
        font.pointSize: 24
        color: palette.text
    }
}
